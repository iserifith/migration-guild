---
name: target-module-bootstrap
description: Scaffold a target module from the active stack pack's declarative project types and templates.
---

# Target Module Bootstrap

Use `guildctl bootstrap`. The engine selects a project type from registry signals and renders the files described by the active stack pack. Preserve existing files and report created and skipped paths.
