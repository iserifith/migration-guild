---
name: legacy-framework-classifier
description: Classify legacy artifacts using the active stack pack's injected classification instructions.
argument-hint: Path to a legacy source file or module root
---

# Legacy Framework Classifier

Use the active stack pack's classification instructions. Inspect the artifact and its module context, record its framework and role, and identify configuration dependencies, target destination, complexity, and tests-first strategy.
