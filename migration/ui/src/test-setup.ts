/**
 * Vitest global setup for the registry UI tests.
 *
 * Imports @testing-library/jest-dom so custom matchers like
 * toBeInTheDocument(), toHaveTextContent(), etc. are available in every
 * test file without explicit imports.
 */
import "@testing-library/jest-dom";
